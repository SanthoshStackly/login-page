import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors (matched to the OneCloud logo)
  static const Color primaryBlue = Color(0xFF1565C0); // main buttons
  static const Color darkNavy = Color(0xFF0D1B3E); // headings / navbar text
  static const Color lightBlue = Color(0xFF2E86F5); // accents / icons
  static const Color skyBlue = Color(0xFF64B5F6); // gradients

  // Neutral colors
  static const Color background = Colors.white;
  static const Color surfaceGrey = Color(0xFFF5F7FA); // cards / sidebar bg
  static const Color borderGrey = Color(0xFFE0E4EA);
  static const Color textDark = Color(0xFF1A1A2E);
  static const Color textGrey = Color(0xFF6B7280);
}
